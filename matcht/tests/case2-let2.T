let
  val g : [one :: <>, two :: <one :: nat, two :: nat -> nat, the :: nat>] =
        in[one :: <>, two :: <one :: nat, two :: nat -> nat>]{one}(<>)
in
  case g {
    one m : <> => z
  | two <one= _ : nat, two = g : nat -> nat> => g z
  }
end
