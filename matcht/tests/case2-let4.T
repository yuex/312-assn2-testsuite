let
  val g : [one :: nat, two :: <one :: nat, two :: nat -> nat>] =
        in[one :: nat, two :: <one :: nat, two :: nat -> nat >]{one}(z)
in
  case g {
    one x : nat => s(x)
  | two <one= _ : nat, two = g : nat -> nat> => g z
  }
end
